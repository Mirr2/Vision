name: dev
description: "A new Flutter project."
publish_to: 'none'  # 이 줄은 pub.dev에 게시하고 싶다면 제거하세요.
version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.9.8+1  # 최신 버전으로 업데이트
  cupertino_icons: ^1.0.6
  tflite_flutter: ^0.9.5  # 최신 버전으로 업데이트

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/detect.tflite
    - assets/labels/labelmap.txt
